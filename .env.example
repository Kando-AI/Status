# 键名登记(值一律不入仓;实例侧存 GitHub Secrets,本地开发存 .env)
# NOTIFY_WEBHOOK_URL — 故障/恢复通知的接收地址(可选;未配置则跳过通知)。获取方式:自建接收端或 Slack Incoming Webhook。
# 探测鉴权头引用的密钥(可选):配置文件 headers 中以 $KEY_NAME 引用,键名自定义,值存 GitHub Secrets 同名条目。
